{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc --build .",
    "clean": "rm -rf ./dist/*",
    "clean:build": "npm run clean && npm run build",
    "lint": "npx eslint .",
    "format": "npx prettier --write .",
    "format:lint": "npm run format && npm run lint",
    "start": "node --expose-gc .",
    "test": "npm run clean:build && node --expose-gc .",
    "test:memory": "node --expose-gc ./dist/memory.js",
    "monitor": "nodemon ."
  },
  "author": "FAR",
  "license": "MIT",
  "engines": {
    "npm": ">=10.2.3",
    "node": ">=18.20.4"
  },
  "dependencies": {
    "@far-analytics/persistence": "file:../.."
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^20.19.39",
    "nodemon": "^3.0.1",
    "typescript": "^5.1.6"
  }
}
